In this example, a non privileged container breaks out to another container (ls, touch or sh in its directory). Our profile manages to prevent this attack.

1. ./set_profile.sh
2. ./1_run_simple_container.sh
3. ./2_run_attacker_to_container.sh
